import { Meta} from '@storybook/addon-docs/blocks';
import theme from '../util/theme.tsx'

<Meta title="DESIGN SYSTEM|Margin"/>

# Margin

마진에 대한 공통 규칙입니다.

## 뷰사이의 간격

뷰 사이의 간격은 `8`과 `12` 입니다.

`R6Br`을 활용하면 마진이 가능합니다 가능합니다.
